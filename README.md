# Zero Lang (Windows)

A high-performance statically typed programming language with zero-cost abstractions and an LLVM backend.

This is the highly-optimized native Windows core. It statically links internal dependencies for maximum zero-latency performance.